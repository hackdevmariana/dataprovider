includes:
    - vendor/nunomaduro/larastan/extension.neon

parameters:
    level: 5
    paths:
        - app
    ignoreErrors:
        - '#^Call to an undefined method [a-zA-Z0-9_\\]+::withAccessToken\(\).#'
